import styled from "styled-components";

const Wrapper = styled.section`
.title, .date-title {
    margin: 1rem 0;
}
.btn {
    margin-top: 1rem;
} .btn-container {
    display:flex;
    gap: 0.5rem;
}
`;
export default Wrapper;